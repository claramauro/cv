# Activer les règles de réécriture
RewriteEngine On

# Si la requête n'est pas un fichier existant
RewriteCond %{REQUEST_FILENAME} !-f
# Et n'est pas un dossier existant
RewriteCond %{REQUEST_FILENAME} !-d

# Rediriger toutes les requêtes vers index.html dans le dossier dist
RewriteRule ^(.*)$ /dist/index.html [L]